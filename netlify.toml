[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Firebase API keys are intentionally public client-side keys.
  # Disable smart detection so the scanner doesn't flag them in the bundle.
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"
